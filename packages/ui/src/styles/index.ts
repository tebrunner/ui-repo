// Re-export the path to globals.css for consumers that need it.
// The actual CSS file is imported directly via `@import` or PostCSS — this
// barrel exists so the styles/ directory follows the same index.ts convention
// as every other layer.
